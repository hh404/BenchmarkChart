# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BenchmarkChart' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BenchmarkChart
  pod 'Charts'
  target 'BenchmarkChartTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BenchmarkChartUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
